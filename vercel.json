{
  "framework": "vite",
  "buildCommand": "pnpm --filter @workspace/publishing-dashboard run build",
  "outputDirectory": "artifacts/publishing-dashboard/dist/public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}